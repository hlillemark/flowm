# @package algorithm.backbone

defaults:
  - base

name: cogv
model_type: cogv

# cogv-small
num_attention_heads: 16
attention_head_dim: 64
num_layers: 8

time_embed_dim: 512

dropout: 0.0
patch_size: 2

use_rotary_positional_embeddings: false
